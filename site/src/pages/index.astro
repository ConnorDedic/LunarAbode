---
---

<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1" />
	<title>Connor Dedic | Offensive Security Researcher</title>
	<style>
		* { margin: 0; padding: 0; box-sizing: border-box; }
		html, body { background: #0c0d10; color: #fefefe; font-family: "Courier New", monospace; height: 100%; overflow: hidden; }
		body { line-height: 1.0; font-size: 14px; }
		.page { display: flex; align-items: center; justify-content: center; height: 100vh; padding: 1rem; }
		pre { white-space: pre; font-family: "Courier New", monospace; font-size: 14px; margin: 0; overflow: auto; }
		a { color: #d10000; text-decoration: none; }
		a:hover { text-decoration: underline; }
		#bbs-root { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: #0c0d10; z-index: 9999; overflow: hidden; display: none; flex-direction: column; }
		#bbs-root.active { display: flex; }
		.bbs-container { flex: 1; display: flex; flex-direction: column; overflow: hidden; }
		#bbs-content { flex: 1; overflow-y: auto; padding: 1rem; font-family: "Courier New", monospace; font-size: 14px; line-height: 1.0; }
		.hint { border-top: 1px solid #333; padding: 0.3rem 1rem; color: #888; font-size: 12px; }
		.invisible-link { position: fixed; top: 0; right: 0; width: 60px; height: 60px; z-index: 999; cursor: pointer; opacity: 0; }
	</style>
</head>
<body>
	<div class="invisible-link" id="bbsLink"></div>
	<div class="page">
		<pre>┌──────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│                           <a href="#">    CONNOR DEDIC    </a>                             │
│                   Offensive Security Researcher & Red Teamer                 │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  Cyber Deception Engineer at Space Dynamics Laboratory (Jan–May 2026).      │
│                                                                              │
│  Specialized in Windows malware development, Active Directory exploitation  │
│  and offensive tooling. Contributing researcher on novel cyber deception    │
│  techniques. SOC analyst, CTF creator. BS Cybersecurity (BYU-Idaho 2026).  │
│                                                                              │
│  [ OFFENSIVE EXPERTISE ]                                                     │
│  └─ Windows malware development, AD exploitation, red team operations       │
│  └─ Impacket, BloodHound, Mimikatz, Empire, reverse engineering             │
│                                                                              │
│  [ DEFENSIVE EXPERTISE ]                                                     │
│  └─ SOC operations (Splunk, Palo Alto, Crowdstrike)                        │
│  └─ Threat hunting, incident response, intrusion detection                  │
│                                                                              │
│  [ TECHNICAL SKILLS ]                                                        │
│  └─ Languages: Python, C, Go, PowerShell, Bash, JavaScript                 │
│  └─ Infrastructure: AWS, Docker, Git, Active Directory                      │
│                                                                              │
│  [ CERTIFICATIONS ]                                                          │
│  └─ AWS Cloud Practitioner, Security+, PenTest+, CySA+, PNPT (incoming)   │
│                                                                              │
│  <a href="mailto:connordedicpro@gmail.com">[ EMAIL ]</a> <a href="https://github.com/ConnorDedic" target="_blank" rel="noopener noreferrer">[ GITHUB ]</a> <a href="https://linkedin.com/in/connor-dedic" target="_blank" rel="noopener noreferrer">[ LINKEDIN ]</a> <a href="/ConnorDedicResume.pdf">[ FULL RESUME ]</a>            │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘</pre>
	</div>

	<div id="bbs-root">
		<div class="bbs-container">
			<pre id="bbs-content"></pre>
			<div class="hint" id="hint">[SPACE] skip</div>
		</div>
	</div>

	<script>
		class BBS {
			constructor() {
				this.state = { mode: 'dial-in', seenDial: this.getSeen() };
				this.root = document.getElementById('bbs-root');
				this.content = document.getElementById('bbs-content');
				this.hint = document.getElementById('hint');
				document.getElementById('bbsLink').addEventListener('click', () => this.enter());
				window.addEventListener('keydown', e => this.handleKey(e));
			}
			getSeen() { try { return localStorage.getItem('bbs:seen') === '1'; } catch(e) { return false; } }
			setSeen() { try { localStorage.setItem('bbs:seen', '1'); } catch(e) {} }
			enter() { this.root.classList.add('active'); this.state.seenDial ? this.menu() : this.dial(); }
			dial() {
				this.state.mode = 'dial-in';
				this.content.textContent = '\n\nCONNECT 56000\nCARRIER ESTABLISHED\nHANDSHAKE...\n\n       ◄ NETRUNNER INCOMING ►\n\n    WELCOME TO THE NETWORK\n    =========================\n\n\n[PRESS ANY KEY TO CONTINUE]\n';
				this.hint.textContent = '[SPACE/ENTER] skip';
			}
			menu() {
				this.state.mode = 'menu';
				this.content.textContent = `██╗   ██╗ ██████╗ ███████╗    ██╗   ██╗██╗██████╗ ███████╗███╗   ███╗██╗   ██╗███████╗
██║   ██║██╔═══██╗██╔════╝    ██║   ██║██║██╔══██╗██╔════╝████╗ ████║██║   ██║██╔════╝
██║   ██║██║   ██║███████╗    ██║   ██║██║██║  ██║█████╗  ██╔████╔██║██║   ██║███████╗
╚██╗ ██╔╝██║   ██║╚════██║    ╚██╗ ██╔╝██║██║  ██║██╔══╝  ██║╚██╔╝██║██║   ██║╚════██║
 ╚████╔╝ ╚██████╔╝███████║     ╚████╔╝ ██║██████╔╝███████╗██║ ╚═╝ ██║╚██████╔╝███████║
  ╚═══╝   ╚═════╝ ╚══════╝      ╚═══╝  ╚═╝╚═════╝ ╚══════╝╚═╝     ╚═╝ ╚═════╝ ╚══════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  [ 1 ] MESSAGE BOARD                                                        │
│  [ 2 ] FILE AREA                                                            │
│  [ 3 ] WHO'S ONLINE                                                         │
│                                                                              │
│  [ 0 ] LOG OFF                                                              │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘`;
				this.hint.textContent = '[1-3] select | [0] exit';
			}
			handleKey(e) {
				if (!this.root.classList.contains('active')) return;
				if (e.key === 'Escape') { this.root.classList.remove('active'); this.state.mode = 'dial-in'; return; }
				if (this.state.mode === 'dial-in' && (e.key === ' ' || e.key === 'Enter')) { this.setSeen(); this.menu(); return; }
				if (this.state.mode === 'menu') {
					if (e.key === '1') this.messageboard();
					else if (e.key === '2') this.filearea();
					else if (e.key === '3') this.whosonline();
					else if (e.key === '0') { this.root.classList.remove('active'); this.state.mode = 'dial-in'; }
				}
				else if (this.state.mode === 'post' && e.key === '0') this.messageboard();
				else if (this.state.mode === 'file' && e.key === '0') this.menu();
				else if (this.state.mode === 'online' && e.key === '0') this.menu();
				else if (this.state.mode === 'file' && e.key === '1') window.location.href = '/ConnorDedicResume.pdf';
				else if (this.state.mode === 'post' && e.key === '1') this.messageboard();
			}
			messageboard() {
				this.state.mode = 'post';
				this.content.textContent = `┌──────────────────────────────────────────────────────────────────────────────┐
│  Active Directory Attacks                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  LAB ENVIRONMENT                                                             │
│  2HYDRA-DC         10.0.2.250                                               │
│  punisher          10.0.2.220                                               │
│  spiderman         10.0.2.221                                               │
│                                                                              │
│  ATTACK METHODOLOGY                                                          │
│  1. Start with mitm6 and responder                                          │
│  2. Scan to generate traffic                                                │
│  3. Check websites for alternate paths                                      │
│  4. Look for default creds                                                  │
│  5. Enumerate further based on findings                                     │
│                                                                              │
│  KERBEROASTING                                                               │
│  Grab SPN hashes:                                                            │
│  impacket-GetUserSPNs <DOMAIN/user:pass> -dc-ip <ip> -request             │
│                                                                              │
│  GOLDEN TICKET                                                               │
│  You need: SID and krbtgt hash                                              │
│  privilege::debug                                                            │
│  lsadump::lsa /inject /name:krbtgt                                          │
│  kerberos::golden /User:<User> /domain:<domain> /sid:<SID>                 │
│                                                                              │
│  [ 1 ] back | [ 0 ] main menu                                              │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘`;
				this.hint.textContent = '[1] back | [0] main menu';
			}
			filearea() {
				this.state.mode = 'file';
				this.content.textContent = `┌──────────────────────────────────────────────────────────────────────────────┐
│  FILE AREA                                                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  [ 1 ] ConnorDedicResume.pdf                                      120 KB   │
│                                                                              │
│  [1] select | [0] main menu                                                │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘`;
				this.hint.textContent = '[1] download | [0] main menu';
			}
			whosonline() {
				this.state.mode = 'online';
				this.content.textContent = `┌──────────────────────────────────────────────────────────────────────────────┐
│  WHO'S ONLINE                                                               │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  > SYSOP (San Diego, CA - idle 2m)                                         │
│  > you (San Diego, CA - active)                                            │
│  > netrunner (Tokyo, JP - active)                                          │
│  > h4x0r (Berlin, DE - idle 8m)                                            │
│  > cipher (London, UK - active)                                            │
│                                                                              │
│  [ 0 ] main menu                                                            │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘`;
				this.hint.textContent = '[0] main menu';
			}
		}
		new BBS();
	</script>
</body>
</html>
